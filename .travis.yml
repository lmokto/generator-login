language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-login' ]; then cd .. && eval "mv $currentfolder generator-login" && cd generator-login; fi
